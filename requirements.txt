# Exact versions used to produce the results reported in the paper.
# Python 3.12.5 on Windows 11 (CPU only; no GPU required).
#
# Install with:
#   python -m venv .venv
#   .venv\Scripts\activate        (Windows)   |   source .venv/bin/activate  (Unix)
#   pip install -r requirements.txt

pandas==3.0.3
numpy==2.4.6
scipy==1.18.0
scikit-learn==1.9.0
matplotlib==3.11.0

# topic modelling stack
bertopic==0.17.4
sentence-transformers==5.6.0
umap-learn==0.5.12
hdbscan==0.8.44

# topic coherence (C_v, NPMI) in 10_sensitivity_coherence.py
gensim==4.4.0

# pulled in by sentence-transformers; pinned so embeddings stay reproducible
torch==2.13.0
transformers==5.14.1
